% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verbs.R
\name{gdpins_pin_write}
\alias{gdpins_pin_write}
\title{Write a pin to a gdpins board}
\usage{
gdpins_pin_write(
  board,
  x,
  name,
  version = NULL,
  format = NULL,
  wkt_engine = NULL
)
}
\arguments{
\item{board}{A \code{gdpins_board} object.}

\item{x}{An R object to pin.}

\item{name}{Character scalar. Pin name (bare snake_case, e.g. \code{"parcels"}).}

\item{version}{Character scalar or \code{NULL}. Version label; \code{NULL} uses the
board default.}

\item{format}{Character scalar or \code{NULL}. One of \code{"parquet"} or \code{"rds"};
\code{NULL} auto-detects via \code{\link[=gdpins_detect_format]{gdpins_detect_format()}}.}

\item{wkt_engine}{Character scalar or \code{NULL}. WKT engine used to encode \code{sf}
geometry: \code{"wk"} (default, fast, full precision) or \code{"sf"} (fallback).
\code{NULL} uses the \code{gdpins.wkt_engine} option. See \code{\link[=gdpins_sf_to_parquet]{gdpins_sf_to_parquet()}}.}
}
\value{
Invisibly \code{NULL}. Called for its side effect.
}
\description{
Serialises \code{x} and writes it to every non-NULL component of \code{board}
(Drive board, cache board, local board). Format auto-detection calls
\code{\link[=gdpins_detect_format]{gdpins_detect_format()}} unless \code{format} is supplied explicitly.
}
\details{
If \code{x} is an \code{sf} object, it is encoded with \code{\link[=gdpins_sf_to_parquet]{gdpins_sf_to_parquet()}}
before writing (type \code{"parquet"}).
}
\examples{
adapter <- gdpins_fake_drive()
board <- gdpins_init_board(
  name       = "data_raw",
  drive_path = "my-project/data-raw",
  cache_dir  = tempfile("cache_"),
  adapter    = adapter,
  create     = TRUE
)

gdpins_pin_write(board, mtcars, "cars")

\dontrun{
adapter <- gdpins_real_drive("1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgVE2upms")
board <- gdpins_init_board(
  name       = "data_raw",
  drive_path = "my-project/data-raw",
  cache_dir  = "~/.cache/gdpins/data-raw",
  adapter    = adapter,
  create     = TRUE
)
gdpins_pin_write(board, mtcars, "cars")
}
}
\seealso{
\code{\link[=gdpins_real_drive]{gdpins_real_drive()}}, \code{\link[=gdpins_init_board]{gdpins_init_board()}}, \code{\link[=gdpins_pin_read]{gdpins_pin_read()}},
\code{\link[=gdpins_pin_remove]{gdpins_pin_remove()}}.
}
