% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output.R
\name{gdpins_publish_output}
\alias{gdpins_publish_output}
\title{Publish local output to Google Drive}
\usage{
gdpins_publish_output(
  tables_board = NULL,
  figures_dir = NULL,
  drive_tables = "output-tables",
  drive_figures = "output-figures",
  adapter = NULL,
  dry_run = FALSE
)
}
\arguments{
\item{tables_board}{A \code{gdpins_board} or \code{NULL}. Source board for output
tables. The read-authoritative local pins board inside \code{tables_board} is
mirrored to Drive (local-first: local > cache > drive board).}

\item{figures_dir}{Character scalar or \code{NULL}. Local directory containing
PNG/SVG figures to publish.}

\item{drive_tables}{Character scalar. Drive destination folder name for
tables. Default \code{"output-tables"}.}

\item{drive_figures}{Character scalar. Drive destination folder name for
figures. Default \code{"output-figures"}.}

\item{adapter}{A \code{gdpins_drive_adapter} or \code{NULL}. If \code{NULL}, uses the
adapter from \code{tables_board}.}

\item{dry_run}{Logical. If \code{TRUE}, show what would be published without
uploading. Default \code{FALSE}.}
}
\value{
Invisibly \code{NULL}. Called for its side effect.
}
\description{
Copies local output (tables board and/or figures directory) to their
corresponding Drive destinations. This is a deliberate, user-triggered
action — local output is never auto-published.
}
\details{
\code{dry_run = TRUE} prints what would be published without uploading anything.
Blocked when offline.
}
\examples{
\dontrun{
adapter <- gdpins_real_drive("1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgVE2upms")

# Publish via adapter directly
gdpins_publish_output(
  figures_dir   = "output/figures",
  adapter       = adapter,
  drive_figures = "my-project/output-figures"
)

# Or pass a board that already holds the adapter
board <- gdpins_init_board(
  name       = "output",
  drive_path = "my-project/output-tables",
  cache_dir  = tempfile("cache_"),
  adapter    = adapter,
  create     = TRUE
)
gdpins_publish_output(tables_board = board, figures_dir = "output/figures")
}
}
\seealso{
\code{\link[=gdpins_real_drive]{gdpins_real_drive()}} to create an adapter.
}
