% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/offline.R
\name{offline-mode}
\alias{offline-mode}
\alias{gdpins_go_offline}
\alias{gdpins_go_online}
\title{Temporarily disconnect from Drive, then reconnect and sync later}
\usage{
gdpins_go_offline(x, ...)

gdpins_go_online(x, adapter = NULL, on_discrepancy = NULL, ...)
}
\arguments{
\item{x}{A \code{gdpins_board} or \code{gdpins_raw_conn} object.}

\item{...}{Passed to methods (currently unused).}

\item{adapter}{A \code{gdpins_drive_adapter} to reconnect with, or \code{NULL}
(default) to reuse the adapter stashed by \code{gdpins_go_offline()} — pass a
freshly authenticated \code{\link[=gdpins_real_drive]{gdpins_real_drive()}} here if the stashed
adapter's credentials have expired.}

\item{on_discrepancy}{Character scalar or \code{NULL}. Same semantics as
\code{\link[=gdpins_init_board]{gdpins_init_board()}}/\code{\link[=gdpins_raw_connect]{gdpins_raw_connect()}}: one of
\code{c("prompt","warn","sync_from_drive","sync_to_drive","ignore")}. \code{NULL}
resolves to \code{"prompt"} interactively or \code{"warn"} non-interactively.}
}
\value{
An object of the same class as \code{x}.
}
\description{
A Drive-backed \code{gdpins_board} or \code{gdpins_raw_conn} normally needs Drive to
be reachable: \code{gdpins_pin_write()} blocks offline writes, and
\code{gdpins_sync()}/\code{gdpins_go_online()} hard-abort without connectivity.
\code{gdpins_go_offline()} and \code{gdpins_go_online()} give users an explicit,
reversible way to work around Drive instability (or work disconnected on
purpose) without losing the ability to reconnect and sync afterwards.
}
\details{
\code{gdpins_go_offline(x)} strips the Drive-facing components off \code{x} and
returns a \code{"local_only"} object backed by whichever local storage \code{x}
already had on disk:
\itemize{
\item \code{"drive_cache_local"} boards keep using their standalone \code{local_board}
/ \code{local_dir} — the same directory the user was already working in.
\item \code{"drive_cache"} boards (no standalone local dir) fall back to their
\code{cache_board} / \code{cache_dir}, mirroring the automatic offline fallback in
\code{\link[=gdpins_init_board]{gdpins_init_board()}}.
\item \code{"drive_local"} raw connections keep using their existing \code{local_path}.
}

No files are copied, moved, or deleted — the returned object reuses the
exact same local \code{pins} board / directory, so anything already on disk
stays reachable, and anything written afterwards lands in the same place.
The original Drive configuration (adapter, drive_path, drive/cache boards)
is stashed on the returned object as an attribute so
\code{gdpins_go_online()} can restore it later. Calling \code{gdpins_go_offline()} on
an object that is already \code{"local_only"} is a no-op.

\code{gdpins_go_online(x)} reverses this: it requires \code{x} to have been produced
by \code{gdpins_go_offline()} (i.e. carry the stashed state), checks
\code{\link[=gdpins_is_online]{gdpins_is_online()}}, reattaches the stashed Drive adapter and path (or a
freshly supplied \code{adapter}), and runs the same discrepancy check used by
\code{\link[=gdpins_init_board]{gdpins_init_board()}} / \code{\link[=gdpins_raw_connect]{gdpins_raw_connect()}} — governed by
\code{on_discrepancy} — so that anything written while offline is reconciled
with Drive.
}
\examples{
adapter <- gdpins_fake_drive()
board <- gdpins_init_board(
  name       = "data_raw",
  drive_path = "my-project/data-raw",
  cache_dir  = tempfile("cache_"),
  local_dir  = tempfile("local_"),
  adapter    = adapter,
  create     = TRUE
)

# Work disconnected for a while -- writes/reads stay local
board_offline <- gdpins_go_offline(board)
board_offline$config   # "local_only"
gdpins_pin_write(board_offline, mtcars, "cars")

# Reconnect and push local changes back up to Drive
board_online <- gdpins_go_online(board_offline, on_discrepancy = "sync_to_drive")
board_online$config    # "drive_cache_local"
}
\seealso{
\code{\link[=gdpins_init_board]{gdpins_init_board()}}, \code{\link[=gdpins_raw_connect]{gdpins_raw_connect()}}, \code{\link[=gdpins_sync]{gdpins_sync()}},
\code{\link[=gdpins_board_status]{gdpins_board_status()}}, \code{\link[=gdpins_is_online]{gdpins_is_online()}}.
}
