% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/board.R
\name{.build_board}
\alias{.build_board}
\title{Do the expensive half of board init}
\usage{
.build_board(spec)
}
\arguments{
\item{spec}{A list from \code{\link[=.board_spec]{.board_spec()}}.}
}
\value{
A fully-resolved \code{gdpins_board}.
}
\description{
Everything that touches the network or the filesystem: the online probe, the
Drive existence/create dance, folder-ID resolution, and \code{pins} board
construction. Called eagerly by \code{\link[=gdpins_init_board]{gdpins_init_board()}} when \code{lazy = FALSE},
and on first component access when \code{lazy = TRUE}.
}
\details{
Deliberately does \strong{not} run the sync check — callers own that, so a lazy
board can mark itself resolved before \code{.handle_init_sync()} reads it back.
}
\keyword{internal}
