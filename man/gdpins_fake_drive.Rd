% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drive-adapter.R
\name{gdpins_fake_drive}
\alias{gdpins_fake_drive}
\title{Create a fake (tempdir-backed) Drive adapter}
\usage{
gdpins_fake_drive(root = tempfile("gdpins_fake_drive_"))
}
\arguments{
\item{root}{Character scalar. Root directory for the fake drive. Defaults to
a fresh \code{tempfile()} path (created on first use).}
}
\value{
An object of class \code{gdpins_drive_adapter}.
}
\description{
Simulates Google Drive using the local filesystem. State (including a trash
store) is held in a mutable environment so all closures share it. Used as
the default test seam — \strong{never hits the network}.
}
\details{
\code{gd_trash()} moves entries to an internal trash store (recoverable). It
never calls \code{unlink()}. \code{gd_md5()} uses \code{tools::md5sum()}. \code{gd_ls()}
excludes trashed entries.
}
\keyword{internal}
