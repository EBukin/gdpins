% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prune.R
\name{gdpins_prune_pin_versions}
\alias{gdpins_prune_pin_versions}
\title{Prune old versions of a single pin}
\usage{
gdpins_prune_pin_versions(
  board,
  name,
  keep = 1,
  dry_run = TRUE,
  threshold = 10,
  force = FALSE
)
}
\arguments{
\item{board}{A \code{gdpins_board} object.}

\item{name}{Character scalar. Pin name.}

\item{keep}{Integer scalar. Number of most-recent versions to keep. Default
\code{1}.}

\item{dry_run}{Logical. If \code{TRUE} (default), show what would be removed
without actually removing anything.}

\item{threshold}{Integer scalar. Maximum number of versions to remove without
requiring \code{force = TRUE} or interactive confirmation. Default \code{10}.}

\item{force}{Logical. If \code{TRUE}, skip the interactive threshold confirmation.
Default \code{FALSE}.}
}
\value{
Invisibly, a character vector of the version labels that were (or
would be) removed (as reported by the primary / Drive board).
}
\description{
Removes old versions of one pin from Drive \strong{and} cache (or local board),
keeping the \code{keep} most recent. Drive versions are always \strong{trashed}
(recoverable via \code{gd_trash()}), never hard-deleted. Cache versions are
deleted from the local filesystem.
}
\details{
Defaults to \code{dry_run = TRUE} for safety: the plan is shown but nothing is
removed.

Deleting more than \code{threshold} versions in a single call requires either
interactive confirmation or \code{force = TRUE}. The threshold check is skipped
during a dry run.

\strong{Raw files are never auto-deleted by any function} -- removal is manual
outside R.
}
