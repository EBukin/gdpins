% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{gdpins_board_status}
\alias{gdpins_board_status}
\title{Report sync status of a board or raw connection}
\usage{
gdpins_board_status(x)
}
\arguments{
\item{x}{A \code{gdpins_board} or \code{gdpins_raw_conn} object.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with at least columns:
\itemize{
\item \code{name} — pin/file name (character).
\item \code{state} — one of \code{"in_sync"}, \code{"local_ahead"}, \code{"drive_ahead"},
\code{"conflict"}, \code{"offline"} (character).
\item Additional signal columns differ by object type (see Details).
}
}
\description{
Returns a per-pin/per-file tibble describing drift between Drive and local.
Dispatches on the class of \code{x}:
}
\details{
\itemize{
\item \strong{\code{gdpins_board}}: compares pins version id/timestamp between the Drive
board and the local side (cache board if present, else local board).
\item \strong{\code{gdpins_raw_conn}}: compares MD5 checksums between the Drive folder and
the local mirror directory; mtime is used as a tiebreaker.
}

If \code{!gdpins_is_online()}, all rows are set to \code{state = "offline"} and a
warning is emitted. No Drive calls are made offline.

\strong{Board signal columns}: \code{drive_version}, \code{local_version}, \code{drive_created},
\code{local_created}, \code{drive_hash}, \code{local_hash}.

\strong{Raw connection signal columns}: \code{drive_md5}, \code{local_md5}, \code{drive_mtime},
\code{local_mtime}.
}
