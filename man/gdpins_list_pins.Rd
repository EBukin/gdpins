% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery.R
\name{gdpins_list_pins}
\alias{gdpins_list_pins}
\title{List all pins in a board}
\usage{
gdpins_list_pins(board)
}
\arguments{
\item{board}{A \code{gdpins_board} object.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{name} (chr), \code{type} (chr),
\code{n_versions} (int), \code{size} (dbl, bytes), \code{modified} (POSIXct).
Returns a zero-row tibble for an empty board.
}
\description{
Returns a programmatic tibble with one row per pin. Compact output fits
≤80 columns. Reads from the board's local-first component
(\code{local_board} > \code{cache_board} > \code{drive_board}).
}
