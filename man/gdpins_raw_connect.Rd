% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raw-connection.R
\name{gdpins_raw_connect}
\alias{gdpins_raw_connect}
\title{Connect to a raw-exogenous Drive folder}
\usage{
gdpins_raw_connect(
  drive_path,
  local_path,
  create = NA,
  on_discrepancy = NULL,
  adapter = NULL
)
}
\arguments{
\item{drive_path}{Character scalar. Drive raw-root path (relative to the
adapter root), or \code{NULL} for \code{"local_only"}.}

\item{local_path}{Character scalar. Local mirror directory path.}

\item{create}{Logical or \code{NA}. Controls Drive folder creation: \code{TRUE} =
create if absent; \code{FALSE} = error if absent; \code{NA} = interactive prompt.}

\item{on_discrepancy}{Character scalar or \code{NULL}. One of
\code{c("prompt","warn","sync_from_drive","sync_to_drive","ignore")}. \code{NULL}
resolves at runtime.}

\item{adapter}{A \code{gdpins_drive_adapter}, or \code{NULL} for \code{"local_only"}.}
}
\value{
A \code{gdpins_raw_conn} object.
}
\description{
Creates a \code{gdpins_raw_conn} object pointing at a Drive raw-root and its
local mirror. On connect, checks for sync discrepancies and whether the
Drive folder exists (controlled by \code{create} and \code{on_discrepancy}).
}
\examples{
# --- Fake adapter (no network) ---
adapter <- gdpins_fake_drive()
conn <- gdpins_raw_connect(
  drive_path = "worldbank-api",
  local_path = tempfile("raw_"),
  adapter    = adapter,
  create     = TRUE
)
conn

# --- Real adapter ---
\dontrun{
adapter <- gdpins_real_drive("1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgVE2upms")
conn <- gdpins_raw_connect(
  drive_path = "worldbank-api",
  local_path = "data/raw/worldbank-api",
  adapter    = adapter,
  create     = TRUE
)

# Supply a Drive folder ID directly as drive_path
conn2 <- gdpins_raw_connect(
  drive_path = "1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgVE2upms",
  local_path = "data/raw/worldbank-api",
  adapter    = adapter
)
}
}
\seealso{
\code{\link[=gdpins_real_drive]{gdpins_real_drive()}} to create an adapter, \code{\link[=gdpins_raw_remove]{gdpins_raw_remove()}}.
}
