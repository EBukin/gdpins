% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lazy.R
\name{gdpins_board_is_connected}
\alias{gdpins_board_is_connected}
\title{Has a board connected yet?}
\usage{
gdpins_board_is_connected(board)
}
\arguments{
\item{board}{A \code{gdpins_board} object.}
}
\value{
\code{TRUE} if the board has connected, \code{FALSE} if it is still lazy.
}
\description{
Reports whether \code{board} has done its deferred init. Eager boards
(\code{lazy = FALSE}) are connected by construction and always return \code{TRUE}.
Never forces a connection itself.
}
\examples{
adapter <- gdpins_fake_drive()
board <- gdpins_init_board(
  name       = "data_raw",
  drive_path = "my-project/data-raw",
  cache_dir  = tempfile("cache_"),
  adapter    = adapter,
  create     = TRUE
)
gdpins_board_is_connected(board)   # FALSE — nothing has touched it

gdpins_pin_write(board, mtcars, "cars")
gdpins_board_is_connected(board)   # TRUE — the write connected it
}
\seealso{
\code{\link[=gdpins_board_connect]{gdpins_board_connect()}}, \link{lazy-boards}.
}
