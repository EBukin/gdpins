% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verbs.R
\name{gdpins_pin_read}
\alias{gdpins_pin_read}
\title{Read a pin from a gdpins board}
\usage{
gdpins_pin_read(board, name, version = NULL)
}
\arguments{
\item{board}{A \code{gdpins_board} object.}

\item{name}{Character scalar. Pin name.}

\item{version}{Character scalar or \code{NULL}. Pin version; \code{NULL} = latest.}
}
\value{
The pinned R object.
}
\description{
Reads from the local-first source: local board if present, else cache board,
else Drive board. Hits the network only if the pin is absent locally.
}
\details{
If the stored object contains \verb{__<epsg>__}-suffixed geometry columns (WKT
encoding), the geometry is automatically restored via \code{\link[=gdpins_parquet_to_sf]{gdpins_parquet_to_sf()}}.
}
\examples{
adapter <- gdpins_fake_drive()
board <- gdpins_init_board(
  name       = "data_raw",
  drive_path = "my-project/data-raw",
  cache_dir  = tempfile("cache_"),
  adapter    = adapter,
  create     = TRUE
)
gdpins_pin_write(board, mtcars, "cars")
gdpins_pin_read(board, "cars")

\dontrun{
adapter <- gdpins_real_drive("1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgVE2upms")
board <- gdpins_init_board(
  name       = "data_raw",
  drive_path = "my-project/data-raw",
  cache_dir  = "~/.cache/gdpins/data-raw",
  adapter    = adapter,
  create     = TRUE
)
gdpins_pin_read(board, "cars")
}
}
\seealso{
\code{\link[=gdpins_real_drive]{gdpins_real_drive()}}, \code{\link[=gdpins_init_board]{gdpins_init_board()}}, \code{\link[=gdpins_pin_write]{gdpins_pin_write()}},
\code{\link[=gdpins_pin_remove]{gdpins_pin_remove()}}.
}
