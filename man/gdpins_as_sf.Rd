% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-formats.R
\name{gdpins_as_sf}
\alias{gdpins_as_sf}
\title{Convert a data frame with a WKT text column to an sf object (autodetecting)}
\usage{
gdpins_as_sf(
  x,
  column = NULL,
  epsg = NULL,
  engine = NULL,
  default_epsg = 4326L
)
}
\arguments{
\item{x}{A data frame with a WKT geometry column.}

\item{column}{Character scalar or \code{NULL}. Name of the WKT column. \code{NULL}
autodetects (see Details).}

\item{epsg}{Integer scalar or \code{NULL}. EPSG code for the geometry CRS. \code{NULL}
infers it from \code{column}'s name (see Details).}

\item{engine}{Character scalar, the WKT engine: \code{"wk"} (default) or \code{"sf"}.
\code{NULL} uses the \code{gdpins.wkt_engine} option. See the \link{io-formats} "WKT
engine" section.}

\item{default_epsg}{Integer scalar. CRS assumed when \code{epsg} is \code{NULL} and the
column name carries no digits. Default \code{4326} (WGS 84).}
}
\value{
An \code{sf} object, or — when autodetection finds no geometry column —
the input \code{x} unchanged (with a warning). A standard \verb{__<epsg>__} suffix is
stripped from the converted column name; other names are kept as-is.
}
\description{
A friendly, single-column decoder for data whose geometry lives in a WKT
character column. Unlike \code{\link[=gdpins_parquet_to_sf]{gdpins_parquet_to_sf()}} — which strictly decodes
every \code{"<name>__<epsg>__"} column and reads the CRS from the name — this
function autodetects the geometry column and infers the CRS, so it also
handles hand-made or externally-produced columns (\code{geom}, \code{geom_1111}, ...)
whose name does not carry a clean EPSG code.
}
\details{
\strong{Column detection.} When \code{column} is \code{NULL}, character columns whose name
matches the standard \verb{__<epsg>__} suffix or contains \code{"geom"}/\code{"wkt"} are
candidates. Exactly one candidate is used automatically. If none is found,
\code{x} is returned unchanged with a warning (so plain, non-spatial data passes
through safely). If several match, an error asks you to pass \code{column}. An
explicitly supplied \code{column} that is absent is always an error.

\strong{CRS inference.} When \code{epsg} is \code{NULL}, the EPSG code is taken from the
column name: the standard \verb{__<epsg>__} pattern is trusted silently; a
non-standard digit run (e.g. \code{geom_1111}) is used but emits a message; a name
with no digits falls back to \code{default_epsg} with a warning. In the latter two
cases you are firmly encouraged to pass \code{epsg} explicitly. An explicit \code{epsg}
argument always wins and silences inference.

No coordinate transformation is performed; the EPSG code only labels the CRS.
}
\examples{
library(sf)
pts <- st_sf(
  id = 1:2,
  geometry = st_sfc(st_point(c(71, 51)), st_point(c(76, 43)), crs = 4326)
)
encoded <- gdpins_sf_to_parquet(pts)   # a "geometry__4326__" WKT column
gdpins_as_sf(encoded)                  # autodetect column + EPSG, silent

# Non-standard name: EPSG inferred from the digits (with a message)
df <- data.frame(geom_3857 = "POINT (0 0)")
gdpins_as_sf(df)

# No CRS in the name: pass epsg explicitly to avoid the default + warning
df2 <- data.frame(geom = "POINT (0 0)")
gdpins_as_sf(df2, epsg = 4326)
}
\seealso{
\code{\link[=gdpins_parquet_to_sf]{gdpins_parquet_to_sf()}} for strict multi-geometry decoding,
\code{\link[=gdpins_sf_to_parquet]{gdpins_sf_to_parquet()}} for the inverse, and \code{\link[=gdpins_pin_read]{gdpins_pin_read()}} whose
\code{wkt_engine = "none"} returns WKT text ready for this function.
}
