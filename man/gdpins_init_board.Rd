% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/board.R
\name{gdpins_init_board}
\alias{gdpins_init_board}
\title{Initialise a gdpins board}
\usage{
gdpins_init_board(
  name,
  drive_path = NULL,
  cache_dir = NULL,
  local_dir = NULL,
  versioned = TRUE,
  create = NA,
  on_discrepancy = NULL,
  adapter = NULL,
  lazy = NULL
)
}
\arguments{
\item{name}{Character scalar. Board/layer label (e.g. \code{"data_raw"}).}

\item{drive_path}{Character scalar. Drive path for the board (relative to
the adapter root), or \code{NULL} for \code{"local_only"}.}

\item{cache_dir}{Character scalar. Local cache directory path, or \code{NULL}.}

\item{local_dir}{Character scalar. Standalone local board directory path, or
\code{NULL}.}

\item{versioned}{Logical. Whether the board stores pin versions. Default
\code{TRUE}.}

\item{create}{Logical or \code{NA}. \code{TRUE} = create Drive board if absent;
\code{FALSE} = error if absent; \code{NA} (default) = interactive CLI prompt or
error.}

\item{on_discrepancy}{Character scalar or \code{NULL}. One of
\code{c("prompt","warn","sync_from_drive","sync_to_drive","ignore")}. \code{NULL}
resolves to \code{"prompt"} interactively or \code{"warn"} non-interactively.}

\item{adapter}{A \code{gdpins_drive_adapter}, or \code{NULL} for \code{"local_only"}.}

\item{lazy}{Logical or \code{NULL}. \code{TRUE} (the default) defers all Drive work
and the sync check until the board is first used; \code{FALSE} does it during
this call. \code{NULL} uses the \code{gdpins.lazy_boards} option (default \code{TRUE}).
See \link{lazy-boards}.}
}
\value{
A \code{gdpins_board} object.
}
\description{
Builds a board in one of three legal configurations depending on the
combination of arguments supplied:
}
\details{
\itemize{
\item \strong{\code{"local_only"}} — \code{local_dir} provided, no \code{drive_path}/\code{adapter}.
\item \strong{\code{"drive_cache"}} — \code{drive_path} + \code{adapter} + \code{cache_dir}, no
\code{local_dir}.
\item \strong{\code{"drive_cache_local"}} — all three: \code{drive_path}, \code{cache_dir}, and
\code{local_dir}.
}

The board checks for sync discrepancies between Drive and local (governed by
\code{on_discrepancy}). Non-existent Drive boards are never auto-created unless
\code{create = TRUE}.

By default (\code{lazy = TRUE}) none of that happens at init: the board records
its arguments and connects on first use. Initialising several Drive boards
is then free, and you only pay for the ones you touch. See \link{lazy-boards} for
what forces a connection and how error timing changes, and
\code{\link[=gdpins_board_connect]{gdpins_board_connect()}} to force one on purpose.
}
\examples{
# --- Fake adapter (no network) ---
adapter <- gdpins_fake_drive()
board <- gdpins_init_board(
  name       = "data_raw",
  drive_path = "my-project/data-raw",
  cache_dir  = tempfile("cache_"),
  adapter    = adapter,
  create     = TRUE
)
board

# --- Real adapter (requires Google Drive auth) ---
\dontrun{
adapter <- gdpins_real_drive("1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgVE2upms")
board <- gdpins_init_board(
  name       = "data_raw",
  drive_path = "my-project/data-raw",
  cache_dir  = "~/.cache/gdpins/data-raw",
  adapter    = adapter,
  create     = TRUE
)

# Supply a Drive folder ID directly as drive_path
board2 <- gdpins_init_board(
  name       = "data_raw",
  drive_path = "1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgVE2upms",
  cache_dir  = "~/.cache/gdpins/data-raw",
  adapter    = adapter,
  create     = TRUE
)
}
}
\seealso{
\code{\link[=gdpins_real_drive]{gdpins_real_drive()}} to create an adapter, \code{\link[=gdpins_go_offline]{gdpins_go_offline()}}
to temporarily detach an existing board from Drive and work locally,
\code{\link[=gdpins_board_connect]{gdpins_board_connect()}} to connect a lazy board on demand.
}
