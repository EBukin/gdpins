% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prune.R
\name{.versions_to_remove}
\alias{.versions_to_remove}
\title{Compute version labels to remove for a given sub-board (oldest, keeping newest)}
\usage{
.versions_to_remove(sub_board, name, keep)
}
\arguments{
\item{sub_board}{A \code{pins} board.}

\item{name}{Pin name.}

\item{keep}{Integer. Number of newest versions to keep.}
}
\value{
Character vector of version labels to remove (may be empty).
}
\description{
\code{pins::pin_versions()} returns rows sorted ascending by \code{created} (oldest
first, newest last). We keep the last \code{keep} rows and remove the rest.
Each sub-board (drive, cache, local) may have slightly different timestamp
prefixes in version labels even for the same logical version, so each board
must compute its own removal list independently.
}
\keyword{internal}
