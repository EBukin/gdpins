% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lazy.R
\name{gdpins_board_connect}
\alias{gdpins_board_connect}
\title{Connect a lazy board now}
\usage{
gdpins_board_connect(board, on_discrepancy = NULL)
}
\arguments{
\item{board}{A \code{gdpins_board} object.}

\item{on_discrepancy}{Character scalar or \code{NULL}. Overrides the value given
at \code{\link[=gdpins_init_board]{gdpins_init_board()}} for this connection only. See
\code{\link[=gdpins_init_board]{gdpins_init_board()}} for the legal values.}
}
\value{
Invisibly \code{board}, now connected.
}
\description{
Forces a board created with \code{lazy = TRUE} (the default) to do its deferred
init: resolve the Drive folder, build the \code{pins} boards, and run the
\code{on_discrepancy} sync check. This is exactly the work
\code{\link[=gdpins_init_board]{gdpins_init_board()}} used to do at init.
}
\details{
Use it to control \emph{when} you pay: call it right after init to restore
eager-style timing and surface a bad \code{drive_path} immediately, or call it at
a natural pause before a long stretch of reads.

Already-connected boards and eager boards are a no-op, so it is safe to call
repeatedly. To only run the sync check on a board that may already be
connected, use \code{\link[=gdpins_board_status]{gdpins_board_status()}} — it connects too, and returns the
per-pin comparison rather than applying a policy.
}
\examples{
adapter <- gdpins_fake_drive()
board <- gdpins_init_board(
  name       = "data_raw",
  drive_path = "my-project/data-raw",
  cache_dir  = tempfile("cache_"),
  adapter    = adapter,
  create     = TRUE
)
gdpins_board_is_connected(board)

gdpins_board_connect(board, on_discrepancy = "ignore")
gdpins_board_is_connected(board)
}
\seealso{
\link{lazy-boards} for what else forces a connection,
\code{\link[=gdpins_board_is_connected]{gdpins_board_is_connected()}}, \code{\link[=gdpins_board_status]{gdpins_board_status()}}, \code{\link[=gdpins_sync]{gdpins_sync()}}.
}
