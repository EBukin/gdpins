# Walk path segments from root_dribble; return the final dribble or NULL

Walk path segments from root_dribble; return the final dribble or NULL

## Usage

``` r
.resolve_real_path(root_dribble, path)
```
