# Print method for gdpins_pin_info

Compact ≤80-col output using `cli` styling. One key-value pair per line.

## Usage

``` r
# S3 method for class 'gdpins_pin_info'
print(x, ...)
```

## Arguments

- x:

  A `gdpins_pin_info` object.

- ...:

  Unused.

## Value

Invisibly `x`.
