# Resolve and validate the WKT engine

Resolve and validate the WKT engine

## Usage

``` r
.gdpins_wkt_engine(engine = NULL)
```

## Arguments

- engine:

  Character scalar `"wk"` or `"sf"`, or `NULL` to fall back to the
  `gdpins.wkt_engine` option (default `"wk"`).

## Value

`"wk"` or `"sf"`.
