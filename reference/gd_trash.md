# Trash a Drive path (recoverable, never hard-deletes)

Trash a Drive path (recoverable, never hard-deletes)

## Usage

``` r
gd_trash(adapter, path)
```

## Arguments

- adapter:

  A `gdpins_drive_adapter` object.

- path:

  Character scalar. Path relative to the adapter root.

## Value

`invisible(adapter)`.
