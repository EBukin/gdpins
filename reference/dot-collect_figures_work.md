# Collect figure publish work items from a local figures directory

Collect figure publish work items from a local figures directory

## Usage

``` r
.collect_figures_work(figures_dir)
```

## Arguments

- figures_dir:

  Character scalar or `NULL`.

## Value

A tibble with columns `local_path` and `file_name`.
