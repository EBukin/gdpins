# Create a directory (and parents) on Drive, idempotent

Create a directory (and parents) on Drive, idempotent

## Usage

``` r
gd_mkdir(adapter, path)
```

## Arguments

- adapter:

  A `gdpins_drive_adapter` object.

- path:

  Character scalar. Path relative to the adapter root.

## Value

`invisible(adapter)`.
