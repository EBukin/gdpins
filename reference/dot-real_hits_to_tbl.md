# Convert a drive_ls dribble to the standard adapter tibble with a path prefix

Convert a drive_ls dribble to the standard adapter tibble with a path
prefix

## Usage

``` r
.real_hits_to_tbl(hits, prefix = "")
```
